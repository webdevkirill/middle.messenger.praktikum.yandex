import { registerComponent } from 'core';

import { ChatContent, ChatMenu } from './components';

registerComponent(ChatMenu);
registerComponent(ChatContent);

export { ChatLayout } from './chatLayout';
